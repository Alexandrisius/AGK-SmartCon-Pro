# Скопируйте в .env и заполните. .env НЕ коммитится (проверьте .gitignore).
PG_PASSWORD=smartcon_dev
HOST_API_PORT=8787
HOST_PG_PORT=5433
JWT_ISSUER=https://cloud.smartcon.local
JWT_AUDIENCE=smartcon-plugin
# 32 байта в hex (64 символа). Сгенерировать: openssl rand -hex 32
JWT_SIGNING_KEY_HEX=CHANGE_ME_64_HEX_CHARS
