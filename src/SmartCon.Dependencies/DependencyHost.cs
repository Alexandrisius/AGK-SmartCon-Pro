namespace SmartCon.Dependencies;

/// <summary>
/// Marker type used to locate the merged SmartCon.Dependencies assembly at runtime
/// (net48 assembly resolution, Issue #134 / ADR-051).
/// </summary>
public static class DependencyHost
{
}
