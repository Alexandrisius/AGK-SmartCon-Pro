<controls:DialogWindowBase x:Class="SmartCon.FamilyManager.Views.ConfirmationDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:SmartCon.UI;assembly=SmartCon.UI"
        xmlns:controls="clr-namespace:SmartCon.UI.Controls;assembly=SmartCon.UI"
        xmlns:converters="clr-namespace:SmartCon.UI.Converters;assembly=SmartCon.UI"
        xmlns:loc="clr-namespace:SmartCon.UI.Localization;assembly=SmartCon.UI"
        Title="{Binding Title}"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}">

    <Window.Resources>
        <ui:SingletonResources/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="16" MinWidth="320">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Message}"
                   Margin="0,0,0,16"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   TextWrapping="Wrap" MaxWidth="400"/>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="{Binding ConfirmText}"
                    Command="{Binding YesCommand}"
                    Width="80" Height="28"
                    IsDefault="True"
                    Style="{DynamicResource PrimaryButton}"/>
            <Button Content="{loc:Loc Btn_No}"
                    Command="{Binding NoCommand}"
                    Width="80" Height="28"
                    Margin="8,0,0,0"
                    IsCancel="True"
                    Style="{DynamicResource SecondaryButton}"
                    Visibility="{Binding IsOkOnly, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}"/>
        </StackPanel>
    </Grid>
</controls:DialogWindowBase>
