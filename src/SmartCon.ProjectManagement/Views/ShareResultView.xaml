<controls:DialogWindowBase x:Class="SmartCon.ProjectManagement.Views.ShareResultView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:SmartCon.UI;assembly=SmartCon.UI"
        xmlns:controls="clr-namespace:SmartCon.UI.Controls;assembly=SmartCon.UI"
        xmlns:loc="clr-namespace:SmartCon.UI.Localization;assembly=SmartCon.UI"
        Title="{loc:Loc PM_ShareResult_Title}"
        Width="440" Height="Auto" MinWidth="380" MaxHeight="480"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}">

    <Window.Resources>
        <ui:SingletonResources/>
    </Window.Resources>

    <Border Style="{DynamicResource CardBorder}" Margin="6">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок: иконка + текст -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="36" Height="36"
                        CornerRadius="18"
                        Background="{DynamicResource SuccessBrush}">
                    <TextBlock Text="✓"
                               Foreground="White"
                               FontSize="20"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
                <TextBlock Margin="12,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           FontSize="15"
                           FontWeight="SemiBold"
                           Text="{loc:Loc PM_ShareResult_Title}"/>
            </StackPanel>

            <!-- Подпись -->
            <TextBlock Grid.Row="1"
                       Margin="0,12,0,2"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="12"
                       Text="{loc:Loc PM_ShareResult_Subtitle}"/>

            <!-- Путь к файлу -->
            <Border Grid.Row="2"
                    Margin="0,4,0,0"
                    Padding="10,8"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderAltBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                <TextBlock Foreground="{DynamicResource AccentBrush}"
                           FontSize="12"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           Text="{Binding SharedFilePath, Mode=OneWay}"/>
            </Border>

            <!-- Статистика -->
            <TextBlock Grid.Row="3"
                       Margin="0,10,0,0"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="12"
                       Text="{Binding StatisticsText, Mode=OneWay}"/>

            <!-- Кнопки -->
            <Grid Grid.Row="4" Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1"
                        Content="{loc:Loc PM_ShareResult_OpenFolder}"
                        Command="{Binding OpenFolderCommand}"
                        Width="120" Height="28"
                        Style="{DynamicResource SecondaryButton}"/>
                <Button Grid.Column="3"
                        Content="{loc:Loc PM_ShareResult_Close}"
                        Command="{Binding CloseCommand}"
                        Width="90" Height="28"
                        IsDefault="True"
                        Style="{DynamicResource PrimaryButton}"/>
            </Grid>
        </Grid>
    </Border>
</controls:DialogWindowBase>
