<controls:DialogWindowBase x:Class="SmartCon.PipeConnect.Views.FittingCtcSetupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:SmartCon.UI;assembly=SmartCon.UI"
        xmlns:controls="clr-namespace:SmartCon.UI.Controls;assembly=SmartCon.UI"
        Title="{DynamicResource Title_CtcSetup}"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow"
        Topmost="True"
        Background="{DynamicResource BackgroundBrush}">

    <Window.Resources>
        <ui:SingletonResources/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,0,8" TextWrapping="Wrap" FontSize="12">
            <Run Text="{DynamicResource Label_Family}"/><Run Text="{Binding FamilyName, Mode=OneWay}" FontWeight="SemiBold" />
            <LineBreak />
            <Run Text="{DynamicResource Label_Symbol}"/><Run Text="{Binding SymbolName, Mode=OneWay}" FontWeight="SemiBold" />
        </TextBlock>

        <TextBlock Grid.Row="1" Margin="0,0,0,8" TextWrapping="Wrap"
                   Text="{DynamicResource Msg_AssignCtc}"
                   Foreground="#666" FontSize="11"/>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding Connectors}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{DynamicResource CardBorder}" Margin="0,3" Padding="8,6">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                             <TextBlock Grid.Row="0" Margin="0,0,0,4" TextTrimming="CharacterEllipsis" FontSize="12">
                                 <Run Text="{DynamicResource Label_Connector}" />
                                 <Run Text="{Binding ConnectorIndex, Mode=OneWay}" />
                                 <Run Text=" (" />
                                 <Run Text="{Binding DisplayText, Mode=OneWay}" />
                                 <Run Text=")" />
                             </TextBlock>

                            <ComboBox Grid.Row="1"
                                      ItemsSource="{Binding DataContext.AvailableTypes, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedItem="{Binding SelectedType, Mode=TwoWay}"
                                      DisplayMemberPath="Name"
                                      Style="{DynamicResource CompactComboBox}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{DynamicResource Btn_Cancel}" Width="80" Margin="0,0,8,0"
                    Command="{Binding CancelCommand}" IsCancel="True"
                    Style="{DynamicResource SecondaryButton}"/>
            <Button Content="{DynamicResource Btn_OK}" Width="80"
                    Command="{Binding SelectCommand}" IsDefault="True"
                    Style="{DynamicResource AccentButton}"/>
        </StackPanel>
    </Grid>
</controls:DialogWindowBase>
