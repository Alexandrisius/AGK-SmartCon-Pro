# Known Workarounds

> Единый реестр всех workaround'ов в проекте SmartCon.
> Загружать: при вопросе «почему этот код делает что-то странное?»
> Каждый workaround ссылается на Issue с полным root cause и rationale.

## Активные workaround'ы

| Issue | Workaround | Файл | Платформа | Описание |
|---|---|---|---|---|
| [#249](https://github.com/Alexandrisius/AGK-SmartCon-Pro/issues/249) | Surface references для labeled dimensions в тестовых сидах | `src/SmartCon.IntegrationTests/FamilyManager/FamilyDefinitionExtractionTests.cs` (Seed) | All (test authoring) | Probe 2026-08-27: размер между ReferencePlane, созданными через API, НЕЛЬЗЯ пометить меткой (FamilyLabel) — ЛЮБОЕ присвоение метки инвалидирует его ссылки при коммите («One or more dimension references are or have become invalid», молчаливый `Commit()==RolledBack` откатывает весь сид без исключения). Workaround: размер строить между ПЛОСКИМИ ГРАНЯМИ экструзии (ComputeReferences=true → PlanarFace.Reference), метку присваивать в ОТДЕЛЬНОЙ транзакции, предпочтительно reporting (instance параметр — type reporting запрещён). |
| [#97](https://github.com/Alexandrisius/AGK-SmartCon-Pro/issues/97) | CS1739 format specifier workaround | All .cs files transitively referencing HelixToolkit/SharpDX | net48 (R19–R24) | HelixToolkit.Wpf.SharpDX 3.1.2 transitively pulls System.Runtime 4.3.0 via SharpDX, конфликтует с PolySharp DefaultInterpolatedStringHandler. `$"{x:F0}"` → `.ToString("F0", CultureInfo.InvariantCulture)`. Permanent workaround. |
| [#129](https://github.com/Alexandrisius/AGK-SmartCon-Pro/issues/129) | Custom zero-dependency GLB writer | `src/SmartCon.FamilyManager/Services/Geometry/FamilyGeometryGlbWriter.cs`, `src/SmartCon.FamilyManager/Services/Geometry/Gltf/*` | All (R19–R25) | Revit 2021 shared AppDomain loads System.Text.Json без `JsonWriterOptions.set_Encoder(...)`, что ломает SharpGLTF.Core 1.0.3 (`MissingMethodException`). Заменён на custom writer без System.Text.Json/Newtonsoft.Json. Permanent fix. |
| [#95](https://github.com/Alexandrisius/AGK-SmartCon-Pro/issues/95) | Off-screen render recovery | `src/SmartCon.App/Diagnostics/BatchDialogRenderRecovery.cs` | net48 (R19–R24) | Белый batch-диалог после OpenDocumentFile + family upgrade. Revit убивает WPF render thread. Окно показывается off-screen, лечится WM_ENTERSIZEMOVE + SetWindowPos + RedrawWindow, затем перемещается на центр при WM_PAINT. |
| — | NoInlining для удалённых API методов | `src/SmartCon.Revit/FamilyManager/SystemCategoryRegistry.PlaceArchitecture.cs` (`PlaceFloorLegacy`, перенесён из SystemCategoryRegistry.cs при разбивке на partial в #260) | R19-R21 binary на 2022+ | Прямая ссылка на удалённый из API метод (`Creation.Document.NewFloor`, удалён в Revit 2022) В ТЕЛЕ любого метода/лямбды убивает JIT ВСЕГО метода на рантайме без этого API: `MissingMethodException` при JIT-компиляции, ДО выполнения любой строки — включая reflection-guard выше (стресс-тест 2026-08-05: потолки с reflection работали, полы с legacy-веткой в той же лямбде — нет). Правило: legacy-вызовы удалённых API — только в отдельном `[MethodImpl(NoInlining)]` методе, reflection/manual scan — в основном. |

| — | Retired→Deprecated status mapping | `src/SmartCon.Core/Models/FamilyManager/ContentStatusParser.cs`, `src/SmartCon.FamilyManager/Services/LocalCatalog/LocalCatalogProvider.cs:447` | All | `ContentStatus` enum сохраняет legacy значение `Retired` (3-й статус из старой 3-state модели) для backward compat с существующими DB-строками. UI показывает только 2 статуса (Active/Deprecated). При чтении из БД строка `"Retired"` маппится в `Deprecated` через `ContentStatusParser.Parse`. Колонка `content_status` в `catalog_items` сохранена без миграции. Без маппинга `Enum.Parse` упал бы на legacy-записях. |
| [#96](https://github.com/Alexandrisius/AGK-SmartCon-Pro/issues/96) | BalloonNudge (DockablePane freeze) | `src/SmartCon.Revit/Util/RevitBalloonNudge.cs` | All (net48 + net8) | REVIT-236376 / REVIT-237190: DockablePane freezes после OpenDocumentFile+Close (bake-in). InfoCenter balloon через AdWindows.dll форсирует Win32 focus event. Не gated to net48 — применяется и на net8 т.к. баг non-deterministic. Финальный балун «Импорт завершён». |
| [#77](https://github.com/Alexandrisius/AGK-SmartCon-Pro/issues/77) | Shared nested persist fallback | `src/SmartCon.Revit/FamilyManager/RevitFamilyTypeCatalogBaker.cs` | All | REVIT-198137: Revit 2023/2024.2 теряет shared nested family names после SaveAs. Имена извлекаются до закрытия документа и сохраняются отдельно. |
| — | VendorId workaround | `src/SmartCon.Revit/.../*Schema*.cs` | All | `SchemaBuilder.SetVendorId` требует ≥4 символа, но `.addin` VendorId="AGK" (3 символа). Решение: использовать `AGKSMARTCON` в Schema + `AccessLevel.Public/Public`. |
| [#209](https://github.com/Alexandrisius/AGK-SmartCon-Pro/issues/209) | Poke + doc-to-doc reload для вложенных | `src/SmartCon.Revit/FamilyManager/RevitFamilyLoadService.cs` (`ReloadNestedInFamilyDocument`, `TryPokeFamilyDocumentForReload`) | All | Revit считает «семейство не изменилось» по dirty-флагу, не по контенту → LoadFamily молча no-op'ит даже при fired-колбэках (Autodesk подтвердил расхождение API/UI, форум 2026-04-30). Net-zero poke источника в памяти (add+remove scratch-параметра, 2 commit, без сохранения) переворачивает штамп → doc-to-doc merge реально заменяет дефиницию. Полный разбор: `docs/family-manager/209-shared-nested-reload-investigation.md` §0. |
| — | PipeConnect modal (Revit API limitation) | `src/SmartCon.PipeConnect/Commands/PipeConnectCommand.cs:33` (`view.ShowDialog()`) | All | Revit принудительно откатывает `TransactionGroup` при возврате из `IExternalCommand.Execute` и `IExternalEventHandler.Execute` (Löbel). Долгоживущий `TransactionGroup` для live real-element preview + single-undo cancel возможен **только** в modal command context. Modeless невозможен. См. [ADR-043](adr/043-pipeconnect-modal-justification.md). Permanent workaround (пока UX не сменится на DirectContext3D preview). |
| [#114](https://github.com/Alexandrisius/AGK-SmartCon-Pro/issues/114) | System.Text.Encoding.CodePages pinned to 8.0.0 | `src/Directory.Packages.props:58` | All | Dependabot bump to 10.0.9 crashes SmartCon on load — `TypeInitializationException` in `LocalFamilyImportService` static constructor (`CodePagesEncodingProvider.Instance` incompatible with net8.0/net48). Pinned to 8.0.0. Add to `.github/dependabot.yml` ignore list. Temporary until SmartCon drops net8.0/net48. |
| [#104](https://github.com/Alexandrisius/AGK-SmartCon-Pro/issues/104) | TemplateCollisionResolver (rename шаблонных типов при staging) | `src/SmartCon.Revit/FamilyManager/TemplateCollisionResolver.cs` | All | `CopyElements` при коллизии имени с типом шаблона копирует тип с авторенеймом («Стена 1»→«Стена 2») даже с `UseDestinationTypes`; удалить шаблонный тип нельзя (запрет удаления последнего типа системной семьи + ремап FamilyName→«Basic Wall» при копии + молчаливый `Commit()==RolledBack`, см. #178). Решение: конфликтующие шаблонные типы переименовываются в `zzSmartConTemplate_*` и остаются в мини-проекте (безвредны: в каталог не попадают, sync не матчится по имени). Permanent workaround. |
| [#151](https://github.com/Alexandrisius/AGK-SmartCon-Pro/issues/151) | UnitSymbolFixup (Autodesk RU localization «бары»→«бар») | `src/SmartCon.Core/Services/Implementation/UnitSymbolFixup.cs` | All (RU Revit) | RU-таблица символов Revit рендерит единицу давления бар как «бары» (по ГОСТ 8.417 «бар» несклоняем). Trailing-token коррекция после `UnitFormatUtils.Format`/`AsValueString` в единой воронке `RevitUnitsCompat.FormatDisplayValue` + 2 fallback-точки. Permanent workaround (пока Autodesk не исправит локализацию). |
| [#153](https://github.com/Alexandrisius/AGK-SmartCon-Pro/issues/153) | Cached-reflection резолв spec параметра для R21 | `src/SmartCon.Revit/Compatibility/RevitUnitsCompat.cs` (`GetSpecTypeId`) | R21 (Revit 2021–2023, net48) | `Definition.GetSpecTypeId()` удалён в Revit 2023, `GetDataType()` добавлен в 2022 — единого compile-time API для диапазона R21 нет; прямой вызов отравляет JIT (MissingMethodException мимо try/catch). Резолв через cached `MethodInfo`: GetDataType → GetSpecTypeId. Permanent workaround. Правило: API, изменённый внутри диапазона конфигурации → reflection, см. `docs/multi-version-guide.md`. |
| — (класс #153) | Cached-reflection портирование shared-параметров для R21 | `src/SmartCon.Revit/FamilyManager/SystemTypeSyncService.Parameters.cs` (`CreatePortedDefinitionOptionsR21`) | R21 (Revit 2021–2023, net48) | Аудит 2.1: `Definition.ParameterType` и ctor `ExternalDefinitionCreationOptions(string, ParameterType)` удалены в Revit 2023; ForgeTypeId-ctor появился только в 2022 — R21-бинарник на Revit 2023 падал MissingMethodException при JIT (симптом тот же, что #153). Cached `ConstructorInfo`/`PropertyInfo`-резолв: modern (string, ForgeTypeId) → legacy enum. See #153 for the pattern. |
| — | ExtrusionRoof вместо NewFootPrintRoof | `src/SmartCon.Revit/FamilyManager/SystemCategoryRegistry.PlaceArchitecture.cs` (`PlaceRoof`, перенесён из SystemCategoryRegistry.cs в #260) | All | `NewFootPrintRoof` требует UI-контекст и бросает `Autodesk.Revit.Exceptions.ArgumentNullException` в фоновом документе (staged мини-проект никогда не активен; доказано зондом в реальном Revit). Крыша строится `NewExtrusionRoof` с ОТКРЫТЫМ двускатным профилем (замкнутый контур отклоняется «Invalid profile») + `ReferencePlane` на первом ViewPlan шаблона (`doc.ActiveView` у фонового документа = null). Permanent workaround (см. ADR-027 «Decisions (Phase 2)» п.3). |

## Устаревшие workaround'ы (удалены)

| Issue | Workaround | Почему удалён | Коммит |
|---|---|---|---|
| — | BalloonNudge pre-ShowDialog | Не предотвращал белый диалог на net48. См. #95. | `f2465f0` |
| — | BalloonNudge в ExtractGeometryPerType | Лишний шум, не решал проблему. | `2ff43d1` |
| — | NullUiFreezeRecoveryService | Заменён на RevitUiFreezeRecoveryService для финального балуна. | `2ff43d1` |
